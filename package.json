{
  "name": "layout_calendar",
  "version": "1.0.0",
  "description": "Display a calendar in the middle of the screen (both horizontally and vertically). Use SCSS and follow BEM. Don't use Javascript.",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html",
    "format": "prettier --ignore-path .prettierignore --write './src/**/*.{html,css,scss}'",
    "lint": "echo 'Lint desativado temporariamente.'",
    "test": "echo 'Testes não aplicáveis neste projeto.'"
  },
  "keywords": [],
  "author": "Mate Academy",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.15.4",
    "parcel": "^2.15.4",
    "prettier": "^3.3.2",
    "sass": "^1.89.2"
  },
  "mateAcademy": {
    "projectType": "layout",
    "linters": {
      "javascript": false
    }
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions",
    "last 2 Edge versions"
  ]
}

